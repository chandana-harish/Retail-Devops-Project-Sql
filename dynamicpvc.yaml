apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: chandana-pvc
  namespace: chandana-ns
spec:
  resources:
    requests:
      storage: 5Gi
  accessModes:
    - ReadWriteMany
  storageClassName: nfs-sc
   

