apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: retail-pvc
  namespace: chandana-ns
spec:
  resources:
    requests:
      storage: 5Gi
  accessModes:
    - ReadWriteMany
  storageClassName: nfs-sc